{
  "name": "ruti",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix ruti-front && npm run build --prefix ruti-front"
  }
}
